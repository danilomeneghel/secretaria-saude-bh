export interface TabelaPiwebOrdemEvtModel {
    /**
     * Valor definido através do atributo [atributoOrdenacao] do modelo PiwebTabelaConfig
     */
    ordem: string;
    /**
     * Nome do atributo do modelo que representa a coluna selecionada na tabela.
     */
    coluna: string;
}
